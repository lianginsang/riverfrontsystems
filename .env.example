# Copy to .env for local reference. Render itself does not read this file,
# it uses the Environment tab in the Render dashboard, set the same two
# variables there for your static site's build step (build.sh reads them).

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key
